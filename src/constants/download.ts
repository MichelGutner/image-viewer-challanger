import { directories } from "@kesha-antonov/react-native-background-downloader";

const DESTINATION_FOLDER = `${directories.documents}`;
export const DOWNLOAD_FOLDER = `${DESTINATION_FOLDER}/.imageViewer`;